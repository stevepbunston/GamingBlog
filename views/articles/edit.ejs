<% layout('layouts/boilerplate') %>
<h1>Edit Article</h1>

<form action="/articles/<%= article._id %>?_method=PUT" method="POST">
    <div>
        <label for="title">Title</label>
        <input type="text" id="title" name="article[title]" value="<%= article.title %>">
    </div>
    <div>
        <label for="game">Game</label>
        <input type="text" id="game" name="article[game]" value="<%= article.game %>">
    </div>
    <div>
    <button>Edit Blog Entry</button>
</form>
<div>
    <a href="/articles/<%= article._id %>">Back</a>
</div>